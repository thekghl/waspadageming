## 1. Reversion Tasks

- [x] 1.1 Revert `lib/services/earthquake_service.dart` to use `http`
- [x] 1.2 Update `lib/main.dart` to use the hardcoded OneSignal ID
- [x] 1.3 Comment out or remove unused database initialization in `DatabaseService`
- [x] 1.4 Update `ApiConfig` to ensure the correct `baseUrl` is active

## 2. Verification

- [ ] 2.1 Verify earthquakes load on the map via the API
- [ ] 2.2 Verify OneSignal logs show successful initialization with the hardcoded ID
