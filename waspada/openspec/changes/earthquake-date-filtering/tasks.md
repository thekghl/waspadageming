## 1. Service Refactor

- [x] 1.1 Modify `EarthquakeService.fetchEarthquakes` to accept `String date`
- [x] 1.2 Update the URI construction to include `?date=$date` and remove the API Key header

## 2. UI Integration

- [x] 2.1 Update `HomePage._fetchEarthquakes` to provide a formatted date string (e.g., today's date)
- [x] 2.2 Verify that markers are still rendered correctly with the new data source
